pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.namespace?.startsWith('org.jetbrains.kotlin')) {
                useVersion kotlinVersion
            }
        }
    }
}

include('core', 'webapp')
